/**
 * Interfaz encargada de imprimir
 */
export interface Printable {
    /**
     * Se encarga de imprimir el resultado.
     * @returns Devuelve el resultado mediante una string
     */
    print(): string;
}
